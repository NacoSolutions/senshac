name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v1
        if: env.ACT != 'true'
        with:
          bun-version: 1.3.13

      - run: bun install --frozen-lockfile
      - uses: flox/install-flox-action@v2
        if: env.ACT != 'true'
      - name: Verify on GitHub runner
        if: env.ACT != 'true'
        uses: flox/activate-action@v1
        with:
          command: bun run verify:ci

      - name: Verify in Act runner image
        if: env.ACT == 'true'
        run: bun run verify:ci

      - name: Test timing summary
        if: always()
        run: bun run report:test-timing

      - name: Code-quality metrics summary
        if: always()
        run: bun run report:quality-metrics

      - name: Upload test timing artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bun-test-timing
          path: .timing.json
          if-no-files-found: ignore
