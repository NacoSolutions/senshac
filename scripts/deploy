#!/usr/bin/env bash
set -euo pipefail

cat >&2 <<'EOF'
Direct Cloudflare Pages deployment is disabled.

Push the verified commit to GitHub and let the repository's GitHub/Cloudflare
Pages integration deploy it. Do not use Wrangler or the cf wrapper locally.
EOF
exit 2
