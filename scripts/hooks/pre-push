#!/usr/bin/env bash
set -euo pipefail

if ! command -v bun >/dev/null 2>&1; then
  echo "bun is not on PATH. Run this push from the Flox environment." >&2
  exit 1
fi

echo "Running thorough pre-push gate..."
bun run check:prepush
