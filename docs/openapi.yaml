# AUTO-GENERATED by `bun run gen:openapi` from `src/pages/`.
# Do not edit by hand. CI fails if this file is out of sync.
openapi: 3.1.0
info:
  title: Senshac HTTP API
  version: 0.0.1
  description: >-
    Auto-generated from `src/pages/`'s `ROUTE_TABLE`. Run `bun run gen:openapi` to refresh; CI fails
    if this schema drifts from the handler module. Request/response bodies are intentionally left
    permissive in V1 — see SPEC §8.1 for the canonical handler contracts.
tags:
  - name: api
    description: '`/api` routes.'
  - name: llms-full.txt
    description: '`/llms-full.txt` routes.'
  - name: llms.txt
    description: '`/llms.txt` routes.'
  - name: meta
    description: Liveness, readiness, and version.
  - name: robots.txt
    description: '`/robots.txt` routes.'
  - name: sitemap.xml
    description: '`/sitemap.xml` routes.'
  - name: tina-island
    description: '`/tina-island` routes.'
paths:
  /api/s3/media:
    get:
      operationId: api/s3/media/index.ts#GET
      tags:
        - api
      summary: GET /api/s3/media
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /api/s3/media/{media}:
    delete:
      operationId: api/s3/media/[...media].ts#DELETE
      tags:
        - api
      summary: DELETE /api/s3/media/:media
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: media
          in: path
          required: true
          schema:
            type: string
  /llms-full.txt:
    get:
      operationId: llms-full.txt.ts#GET
      tags:
        - llms-full.txt
      summary: GET /llms-full.txt
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /llms.txt:
    get:
      operationId: llms.txt.ts#GET
      tags:
        - llms.txt
      summary: GET /llms.txt
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /robots.txt:
    get:
      operationId: robots.txt.ts#GET
      tags:
        - robots.txt
      summary: GET /robots.txt
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /sitemap.xml:
    get:
      operationId: sitemap.xml.ts#GET
      tags:
        - sitemap.xml
      summary: GET /sitemap.xml
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /tina-island/{name}:
    all:
      operationId: tina-island/[name].ts#ALL
      tags:
        - tina-island
      summary: ALL /tina-island/:name
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
  /{lang}/api/contact:
    all:
      operationId: '[lang]/api/contact.ts#ALL'
      tags:
        - meta
      summary: ALL /:lang/api/contact
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: lang
          in: path
          required: true
          schema:
            type: string
    post:
      operationId: '[lang]/api/contact.ts#POST'
      tags:
        - meta
      summary: POST /:lang/api/contact
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: lang
          in: path
          required: true
          schema:
            type: string
